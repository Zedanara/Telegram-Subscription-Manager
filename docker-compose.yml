services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "${DB_HOST_PORT:-5433}:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5

  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    # Not published to the host: Caddy (below) is the only public entry
    # point now, terminating HTTPS and reverse-proxying to bot:8000 over
    # the Docker network. "expose" documents the intent; Compose already
    # lets sibling containers reach this port without it.
    expose:
      - "${WEBHOOK_PORT:-8000}"
    volumes:
      # Content for "Посмотреть примеры контента" — uploaded straight to the
      # host, never committed (see media/examples/README.md). Read-only from
      # the bot's side so a compromised container can't write into it.
      - ./media/examples:/app/media/examples:ro
    depends_on:
      db:
        condition: service_healthy

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
    depends_on:
      - bot

volumes:
  db_data:
  caddy_data:
